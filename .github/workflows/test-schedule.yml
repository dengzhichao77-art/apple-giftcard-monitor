name: Test Schedule

on:
  schedule:
    - cron: '*/2 * * * *'  # 每2分钟一次
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Print timestamp
      run: |
        echo "🕐 测试运行时间: $(date)"
        echo "✅ 如果看到这个，说明调度正常"
